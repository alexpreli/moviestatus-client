window.MOVIESTATUS_CONFIG = {
  tmdbApiKey: "YOUR_TMDB_API_KEY_HERE",
  region: "US",
  tmdbLanguage: "en-US",
  tmdbSearchDailyLimit: 50, // default 100
  searchVerifyBaseUrl: "", // optional
  localServerPort: 3000,
  searchVerifyDailyLimit: 0, // default 100
  listingCheck: true,
};